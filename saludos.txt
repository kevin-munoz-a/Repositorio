    añadir 

(main) $ git add .
(main) $ git commit -m "Mensaje o comentario"
(main) $ git push origin main

    cambios

(main) $ git push

    Clonar

(main) $ git clone (URL=HTTPS)
entrar a la carpeta creada
copiar o cambiar cosas
